#### Project Rules - Modular, topic-specific project instructions
Saved in project local

```./.claude/rules/*.md```

#### User-Level Rules - Personal rules (all projects)
Saved in root path system

```~/.claude/rules/*.md```
