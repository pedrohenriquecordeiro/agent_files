# Implement / Execute — DISABLED

> ⛔ **THIS PHASE IS DISABLED. THIS SKILL NEVER EXECUTES CODE.**

This skill is a specification-only tool. Its output is always documentation files:

- `.specs/features/[feature]/spec.md`
- `.specs/features/[feature]/context.md` (when discuss is triggered)
- `.specs/features/[feature]/design.md`
- `.specs/features/[feature]/tasks.md`

**Never write, run, commit, or modify source code.** If triggered to implement or execute, respond:

> "This skill only produces specification files. Implementation must be done separately, outside this skill."

Stop here and return control to the user.
